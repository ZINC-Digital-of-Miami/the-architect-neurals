# App, mobile, and report-navigation audit — September 8, 2026

Scope: all 23 published HTML routes; responsive navigation; map interaction and failure states; rendered research coverage; report references; preservation; sharing and print exports. This audit does not newly verify every historical or political assertion against external primary records.

## Confirmed defects and repairs

| Source | Mechanism and user-visible failure | Verdict and repair |
| --- | --- | --- |
| `src/neural_map.html:673` | Phones selected Entities automatically; the network was hidden. Once enabled, the graph also followed a full screen of controls. | CONFIRMED. Network opens first; on phones its actual DOM position follows Search/View. Desktop order is restored on resize. |
| `src/neural_map.html:1206` | A fitted 59-node graph had tiny targets and no pinch handling. | CONFIRMED. Pinch zoom and a larger zoom range preserve ordinary taps, panning, and entity identity. |
| `src/neural_map.html:1466` | The entity sheet captured Tab while the native Share dialog was open. | CONFIRMED. Native dialogs own focus; closing Share preserves the selected entity. |
| `src/neural_map.html:649` | The no-results message was inside the hidden entity-list container when Network was active. | CONFIRMED. Empty results are visible in either mode. Data-load failure now disables unavailable controls; SVG and topic-index failures retain their independent fallbacks. |
| `src/final.css:406` and `:583` | Menu/topic targets and some section/footer links were too small; mobile body type was 17px. | CONFIRMED. Menu, Contents and affected utility links have at least 44px targets; body type is 18px. Long URLs and revision records wrap. |
| `src/build_site3.py:77` and `:373` | The report index omitted 68 H4 headings. Repeated weekly headings lacked dated anchors. | CONFIRMED. All 154 master headings and 24 dated brief headings are indexed; existing published IDs remain. |
| `src/build_site3.py:406` | Sources exposed a code-formatted historical archive but omitted the 36 registered source URLs. | CONFIRMED. All 36 records are linked and the historical archive renders as readable prose. |
| `src/build_research.py:244` and `:288` | Connections showed only four curated paths; 20 reviewed relationships and all 13 revisions lacked their own rendered entries there. | CONFIRMED. All 31 reviewed relationships and all 13 revision records are exposed, with sources and retained history. |
| `src/report_references.py:264` | Named chapter/appendix references were plain text; two Chapter 21 references pointed conceptually to an unnumbered Budget Architecture heading. | CONFIRMED. 138 occurrences resolve to actual headings, including the explicit budget alias. Unknown, ambiguous or split-markup references fail the build for review. |
| `src/report_navigation.py:6` | Three Brief 002 phrases said corrections were “below,” although those records are elsewhere. The source archive similarly said Chapter 21-C was “below.” | CONFIRMED. Navigation wording now points to exact C-010/C-011/C-012 anchors and Chapter 21-C in the main report. Original authored files remain unchanged. |
| `src/report_navigation.py:35` | Several Silence Ledger cards sent readers to an unrelated weekly section. | CONFIRMED. Five destinations corrected; five unsupported destinations become explicit nonlinks. All 12 clock bodies remain. |

## Preservation and coverage

- The master report, original source index and three dated source briefs match their pre-change bytes and hashes.
- The complete research registry, update source and map source also match their pre-change hashes. All 59 nodes, 93 edges, 18 correction blocks (17 distinct IDs), 35 claims and 13 revisions remain.
- The complete outline contains 184 rows: 154 master headings, 24 dated brief headings and six current/main headings.
- Repeated weekly titles are legitimate headings in distinct dated editions. No duplicate IDs or repeated whole-report insertion was found. The initial immediate deep-link test failures were disproved: the test measured before the browser processed hash scrolling.
- Contextual master-reference inspection classified 64 occurrences: 26 document-location uses and 38 ordinary uses. No wrong direction was found. Three refer to broad spans; one uses related terminology rather than verbatim matching names. Archive inspection located the four direction defects repaired above; it is not a factual review of every enclosing assertion.

## Verification

- `python3 -m unittest discover -s tests -p 'test_*.py'`: 56 tests.
- `python3 -m unittest discover -s scripts -p 'test_guards.py'`: 41 release/preservation guard tests.
- `node tests/test_mapgen.js`: nine map-generator cases.
- `python3 scripts/check_section_links.py`: 23 pages, 3,913 local links, zero duplicate IDs or missing destinations.
- `node tests/test_app_flows.cjs`: 43 checks, all 154 master-heading deep links, 69 route/viewport measurements, zero browser or local HTTP errors.
- `node tests/test_map_touch.cjs`: map visible on the first viewport at 319/390px, pinch in/out, no accidental entity selection, readable zoom, correct entity tap, Share focus/Escape and visible empty state.
- `node tests/test_map_failures.cjs`: normal load and independent registry/SVG/entity-data failures.
- Additional browser checks: all six Synthesis views; all 31 relationship entries; all 30 expanded revision detail panels at 319/390px without horizontal overflow; menu open/Escape and topic filters.
- `node scripts/build_print_pdfs.cjs`: all 103 route-specific PDFs regenerated. Text/page/link validation covers all 103; evidence checks cover 233 claim/relationship occurrences. Full-report extraction contains all 1,314 tested text passages.
- `./check.sh`: source preservation, brief lint, registry integrity, every generated byte compared with an isolated rebuild, and source-bound PDF manifests.

Browser touch checks use Chrome mobile emulation and CDP touch input. Physical-device Safari behavior is not measured. Production delivery is accepted only after live GitHub main and the deployment SHA match and direct production browser checks pass.

## Research gaps retained explicitly

The 62 historical map relationships without attached structured source records remain visible with their existing labels; no evidence has been invented for them. Five Silence Ledger cards still lack an identified separate supporting section. Three topics have no assigned mapped entities. These are research coverage gaps, not deleted data. They require targeted source work before additions can be asserted.
